# Add Exponentiation Function

Add a new mathematical helper to the public API.

- [x] Implement a new exported function `potencia(base: number, expoente: number): number` in `src/index.ts`
- [x] Follow the existing coding style and export conventions
- [x] Use the built-in JavaScript exponentiation operator (`**`)
- [x] Preserve all existing exported functions without modification

# Improve Mathematical Helpers

Continue evolving the module with small, independent improvements.

- [ ] Add a new exported function `raizQuadrada(valor: number): number` using `Math.sqrt`
- [ ] Add a new exported function `valorAbsoluto(valor: number): number` using `Math.abs`
- [ ] Add a new exported function `maximo(a: number, b: number): number` using `Math.max`
- [ ] Add a new exported function `minimo(a: number, b: number): number` using `Math.min`
- [ ] Add a new exported function `media(a: number, b: number): number` returning the arithmetic mean
- [ ] Keep all existing exports unchanged
- [ ] Preserve the existing formatting and coding conventions