{
  "scenario": "bash",
  "source_analysis": "bin/deploy.sh sources the only bridge (to lib/utils.sh) but is not classified as an entrypoint or hotspot. lib/utils.sh is the sole boundary. Both are uncovered (test_covered=False). target_system_profile.yml is an isolated config file (non-code, not an interpretation target). Paths are canonical builder form; the scorecard strips a leading input/ prefix when comparing.",
  "expected_interpretation_targets": [
    "bin/deploy.sh",
    "lib/utils.sh"
  ],
  "expected_repair_candidates": [
    "bin/deploy.sh",
    "lib/utils.sh"
  ],
  "target_basis": {
    "bin/deploy.sh": "uncovered bridge source and deployment orchestrator — defect propagates across the sole structural bridge to the boundary",
    "lib/utils.sh": "uncovered inbound boundary — the structural anchor of the topology with no test validation"
  }
}
