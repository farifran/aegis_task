{
  "scenario": "python",
  "source_analysis": "main.py is the sole entrypoint and hotspot (out_degree=2), importing auth and billing. auth.py is a covered boundary; billing.py is an uncovered boundary. test_auth.py is a test file that is structurally isolated (total_degree=0) — it does not import the module it tests. Paths are canonical builder form (relative to the scenario input dir); the scorecard strips a leading input/ prefix when comparing.",
  "expected_interpretation_targets": [
    "src/main.py",
    "src/auth.py",
    "src/billing.py",
    "tests/test_auth.py"
  ],
  "expected_repair_candidates": [
    "src/main.py",
    "src/billing.py",
    "tests/test_auth.py"
  ],
  "target_basis": {
    "src/main.py": "uncovered dependency hub, sole entrypoint and hotspot, bridge origin — defect propagates to both boundaries",
    "src/billing.py": "uncovered boundary receiving inbound bridge from the hub — untested dependency chain endpoint",
    "tests/test_auth.py": "isolated test file (total_degree=0) not exercising the module it names — structural wiring defect"
  }
}
