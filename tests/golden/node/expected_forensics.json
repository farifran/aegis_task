{
  "scenario": "node",
  "source_analysis": "src/index.ts is the sole entrypoint and the bridge endpoint (db.ts → index.ts). src/db.ts is the sole boundary and bridge source. Both are uncovered (test_covered=False). package.json, tsconfig.json, and target_system_profile.yml are isolated config files (non-code, not interpretation targets). Paths are canonical builder form; the scorecard strips a leading input/ prefix when comparing.",
  "expected_interpretation_targets": [
    "src/index.ts",
    "src/db.ts"
  ],
  "expected_repair_candidates": [
    "src/index.ts",
    "src/db.ts"
  ],
  "target_basis": {
    "src/index.ts": "uncovered entrypoint and bridge endpoint — the runtime entry surface with no test validation, receiving the sole structural bridge",
    "src/db.ts": "uncovered boundary and bridge source — the structural anchor of the topology, untested dependency chain origin"
  }
}
