{
  "scenario": "monolith",
  "source_analysis": "users_controller.py imports app.models.user; user.py is a leaf model. users_controller.py is the sole dependency source (out_degree=1, bridge endpoint) — topologically equivalent to bash deploy.sh and node index.ts, which are both declared hotspots. Paths relative to scenario input (matches builder output format).",
  "expected_bridges": [
    {"from": "app/controllers/users_controller.py", "to": "app/models/user.py"}
  ],
  "expected_boundaries": [
    {"file": "app/models/user.py"}
  ],
  "expected_hotspots": [
    {"file": "app/controllers/users_controller.py"}
  ],
  "expected_entrypoints": [],
  "expected_surfaces": [
    {"members": ["app/controllers/users_controller.py", "app/models/user.py"]}
  ]
}
