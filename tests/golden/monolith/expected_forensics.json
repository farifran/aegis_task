{
  "scenario": "monolith",
  "source_analysis": "users_controller.py imports app.models.user; user.py is a leaf model and the sole boundary. users_controller.py is the bridge source and hotspot. No entrypoint was detected. test_users.py is a test file structurally isolated (total_degree=0) — it does not import the controller or model it tests. Paths are canonical builder form; the scorecard strips a leading input/ prefix when comparing.",
  "expected_interpretation_targets": [
    "app/controllers/users_controller.py",
    "app/models/user.py",
    "tests/test_users.py"
  ],
  "expected_repair_candidates": [
    "app/controllers/users_controller.py",
    "app/models/user.py",
    "tests/test_users.py"
  ],
  "target_basis": {
    "app/controllers/users_controller.py": "uncovered sole bridge source — zero test coverage at the dependency origin, defect propagates across the bridge to the boundary",
    "app/models/user.py": "uncovered inbound boundary — the structural anchor of the topology with no test validation",
    "tests/test_users.py": "isolated test file (total_degree=0) not exercising the controller/model it names — structural wiring defect"
  }
}
